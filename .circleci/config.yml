version: 2.1
jobs:
  build:
    docker:
      - image: circleci/python:3.8
    steps:
      - checkout
      - run:
          name: Say Hello
          command: echo "Hello, World!"
workflows:
  version: 2
  hello-world-workflow:
    jobs:
      - build
